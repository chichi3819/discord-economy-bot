{
  "name": "discord-economy-bot",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun",
    "deploy": "bun run src/deploy-commands.ts",
    "setup-check": "bun run setup-check.ts",
    "lint": "echo 'No linting configured yet'"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "mongoose": "^8.8.4",
    "dotenv": "^16.4.7"
  }
}